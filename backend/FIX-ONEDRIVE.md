# 🔧 Решение проблемы с OneDrive и Prisma

## Проблема
OneDrive блокирует файлы `.dll.node` Prisma, что мешает генерации Prisma Client.

## ✅ Решение: Исключить node_modules из синхронизации

### Способ 1: Через интерфейс OneDrive (Рекомендуется)

1. **Откройте настройки OneDrive:**
   - Кликните на иконку OneDrive в трее (правый нижний угол)
   - Выберите "Параметры" → "Учетная запись"
   - Нажмите "Управление резервным копированием"

2. **Исключите папку node_modules:**
   - В разделе "Дополнительно" найдите "Исключить папки"
   - Добавьте путь: `Desktop\duplicate-hipo\backend\node_modules`
   - Или добавьте глобально: `**\node_modules` (исключит все node_modules)

3. **Альтернативный способ:**
   - Правый клик на папку `node_modules`
   - Выберите "OneDrive" → "Освободить место" (Files On-Demand)
   - Это сделает папку доступной только локально

### Способ 2: Через PowerShell (Автоматически)

Запустите PowerShell **от имени администратора** и выполните:

```powershell
# Перейти в папку проекта
cd "C:\Users\Gurgen\OneDrive\Desktop\duplicate-hipo\backend"

# Установить атрибут "не синхронизировать" для node_modules
attrib +N "node_modules" /S /D
```

### Способ 3: Временное решение - Перегенерировать Prisma

После исключения node_modules выполните:

```bash
# Очистить старые файлы Prisma
rm -rf node_modules\.prisma
rm -rf node_modules\@prisma\.engines-*

# Перегенерировать Prisma Client
npm run prisma:generate
```

## 🚀 После настройки

1. Перезапустите терминал
2. Выполните: `npm run prisma:generate`
3. Запустите сервер: `npm run dev`

## 📝 Примечание

`node_modules` не нужно синхронизировать в OneDrive, так как:
- ✅ Он очень большой (сотни МБ)
- ✅ Его можно восстановить через `npm install`
- ✅ Он часто меняется при установке пакетов
- ✅ Синхронизация замедляет работу

## 🔄 Если проблема осталась

Попробуйте:
1. Закрыть все процессы Node.js
2. Перезапустить OneDrive
3. Выполнить генерацию от имени администратора


